# Leave empty until you are ready to test live drafts.
# OpenRouter keys start with sk-or-
OPENROUTER_API_KEY=
# Optional. Defaults to openai/gpt-4o-mini.
OPENROUTER_MODEL=
# Optional. Phone and desktop recordings use this. Defaults to openai/whisper-1.
OPENROUTER_STT_MODEL=
